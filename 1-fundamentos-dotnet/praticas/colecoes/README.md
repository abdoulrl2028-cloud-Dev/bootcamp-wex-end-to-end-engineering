# Prática: Coleções

Exemplo com `List<T>` e `Dictionary<TKey,TValue>`.

Como executar:

```bash
cd 1-fundamentos-dotnet/praticas/colecoes
dotnet run
```

Saída esperada:

```
Lista:
- maçã
- banana
- laranja
Dicionário:
- um = 1
- dois = 2
Filtradas (começam com 'b'):
- banana
```
